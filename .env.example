# Database Configuration
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=
DB_NAME=
DB_PASSWORD=

# Security (REQUIRED in Production)
# Generate strong random strings for these
JWT_SECRET=
TOKEN_PEPPER=

# CORS Configuration (REQUIRED in Production)
# Comma-separated list of allowed origins (e.g., https://yourdomain.com)
ALLOWED_ORIGINS=

# Server Configuration
PORT=3000
NODE_ENV=production
TRUST_PROXY=1
METRICS_KEY=

# Initial Admin Setup (Optional)
ADMIN_EMAIL=admin@thouesa.com
ADMIN_PASS=Admin123!
